import { Meteor } from 'meteor/meteor';
//Creo que aca se puede hacer el import sin espeficar la className
//Algo tipo "../imports/api/programs.js"
import {Programs} from "../imports/api/programs";

Meteor.startup(() => {
  // code to run on server at startup


});
